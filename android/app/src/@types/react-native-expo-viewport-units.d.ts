declare module 'react-native-expo-viewport-units' {
    const vw: (value: number) => number;
    const vh: (value: number) => number;
    const vmin: (value: number) => number;
    const vmax: (value: number) => number;
    export { vw, vh, vmin, vmax };
}